<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gt.florian.colazoprueba">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PrimerosPasos"
            />
        <activity android:name=".AyudaAsistencia" />
        <activity android:name=".Ajustes" />
        <activity android:name=".TerminosCondiciones" />
        <activity android:name=".Creditos"></activity>
    </application>

</manifest>
